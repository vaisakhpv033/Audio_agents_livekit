[project]
name = "report-server"
version = "0.1.0"
description = "A FastAPI server to store and visualize LiveKit session reports"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "jinja2>=3.1.3",
    "pydantic>=2.6.4",
    "markdown>=3.6",
    "python-dotenv>=1.2.2",
    "httpx>=0.28.1",
]
